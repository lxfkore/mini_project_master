import 'package:flutter/material.dart';

// Define your colors
const Color blackColor = Color.fromRGBO(30, 30, 30, 1);
const Color yellowColor = Color.fromRGBO(254, 198, 51, 1);
const Color textColor = Color.fromRGBO(72, 26, 26, 1);

// Define border radii
const BorderRadius ellipticalBorderRadius = BorderRadius.all(Radius.elliptical(109, 112));

